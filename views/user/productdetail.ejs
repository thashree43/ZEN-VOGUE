
<%- include("../layouts/userpartials/header.ejs") %>
 <!-- Include jQuery -->
 <script src="https://cdnjs.cloudflare.com/ajax/libs/jquery/3.6.0/jquery.min.js"></script>
 <!-- Include Elevate Zoom -->
 <script src="https://cdnjs.cloudflare.com/ajax/libs/elevatezoom/3.0.8/jquery.elevatezoom.min.js"></script>
    <style>
         .hero-area {
      background: url("/path/to/hero-image.jpg") no-repeat center;
      background-size: cover;
      padding: 200px 0;
      color: #b43333;
      text-align: center;
      background: #e0e0e0;
    }

    .hero-content h2 {
      font-size: 36px;
      font-weight: bold;
      margin-bottom: 20px;
    }

    .breadcrumb-area {
      padding: 20px 0;
      background-color: #e6e6e6;
    }

    .breadcrumb-content h1 {
      font-size: 24px;
      font-weight: bold;
      margin-bottom: 10px;
      color: #000000;
    }

    .breadcrumb-content nav {
      font-size: 16px;
    }

    .single-product-area {
      margin-top: 50px;
    }

    .product-carousel .single-product-item img {
      max-width: 100%;
    }

    .product-info h3 {
      font-size: 38px;
      font-weight: bold;
      margin-bottom: 35px;
    }

    .product-info h2 {
      font-size: 36px;
      font-weight: bold;
      color: #092399;
      margin-bottom: 15px;
    }

    .product-info ul.list {
      margin-bottom: 20px;
    }

    .product-info ul.list li {
      font-size: 16px;
      color: #000000;
      margin-bottom: 5px;
    }

    .product-info p {
      font-size: 16px;
      color: #000000;
      margin-bottom: 20px;
    }


    .subscribe-part {
      padding: 50px 0;
      width: 100%;
      background-color: #191d4d;
      text-align: center;
      margin-top: 30px;
    }

    .subscribe-content h2 {
      font-size: 32px;
      font-weight: bold;
      margin-bottom: 20px;
      color: #ffffff;
    }

    .subscribe-content p {
      font-size: 16px;
      color: #ffffff;
      margin-bottom: 30px;
    }

    .subscribe-form input {
      padding: 10px 25px;
      border: none;
      border-radius: 30px;
      background-color: #f1d7d7;
    }

    .subscribe-form .btn_1 {
      padding: 12px 25px;
      border: none;
      background-color: #ff0000;
      color: #ffffff;
      border-radius: 30px;
      text-transform: uppercase;
      transition: background-color 0.3s ease;
    }

    .subscribe-form .btn_1:hover {
      background-color: #222;
    }

    .nav-tabs .nav-item {
      margin-bottom: 10px;
    }

    .product__thumb__pic img {
      max-width: 100%;
      height: auto;
      border: 1px solid #ddd;
      transition: transform 0.2s;
      cursor: pointer;
    }

    .product__details__pic__item {
      max-width: 100%;
      padding: 80px;
      height: auto;
      transition: transform 0.2s;
      cursor: pointer;
    }

    .zoom-image:hover {
      transform: scale(1.1);
      border-color: #ff9900;
    }


    .container {
      display: flex;
    }

    .quantity {
      flex-direction: column;
      display: flex;
      padding: 20px;
    }

    .input-number {
      width: 50px;
      padding: 5px;
      border: 1px solid #ccc;
      border-radius: 5px;
      text-align: center;
      font-size: 14px;
    }

    .quantity-controls {
      display: flex;
      align-items: center;
    }

    .quantity-controls button {
      padding: 7px 10px;
      border: none;
      background-color: #000000;
      font-size: 12px;
      cursor: pointer;
      transition: background-color 0.2s;
    }

    .quantity-controls button:hover {
      background-color: #616161;
    }

    .product-actions {
      display: flex;
      flex-direction: row;
      align-items: center;
    }

    .fa-heart {
  color: #ff0000;
  margin: 20px;
  font-size: 32px; 
  transition: color 0.3s; 
  cursor: pointer; 
}

.fa-heart:hover {
  color: #59ec14; 
}

.fa-heart:active {
  transform: scale(1.2); 
}


.review-container {
  max-width: 800px;
  margin: 0 auto;
  padding: 20px;
  background-color: #f9f9f9;
  border-radius: 10px;
  box-shadow: 0 0 10px rgba(168, 20, 20, 0.1);
}

.rating-title {
  font-size: 38px;
  font-weight: bold;
  color: #0004ffe1;
  text-align: center;
  margin-top: 100px;
  margin-bottom: 30px;
}

.review-form {
  text-align: center;
  margin-top: 20px;
  background-color: #f9f9f9;
  border-radius: 10px;
  padding: 20px;
  box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
}

.rating > input{ display:none;}

.rating > label {
position: relative;
width: 1.1em;
font-size: 20px;
color: #FFD700;
cursor: pointer;
}

.rating > label::before{
content: "\2605";
position: absolute;
opacity: 0;
}

.rating > label:hover:before,
.rating > label:hover ~ label:before {
opacity: 1 !important;
}

.rating > input:checked ~ label:before{
opacity:1;
}

.rating:hover > input:checked ~ label:before{ opacity: 0.4; }

.rating {
  display: flex;
  justify-content: center;
  align-items: center;
  margin-bottom: 20px;
flex-direction: row-reverse;

}

.input-group {
  display: flex;
  flex-direction: column;
  margin-bottom: 20px;
  width: 100%;
}

textarea {
  width: 100%;
  padding: 10px;
  border: 1px solid #ccc;
  border-radius: 5px;
  font-size: 16px;
  margin-top: 10px;
}

.error-message {
  font-size: 14px;
  margin-top: 5px;
}

.submit-button {
  background-color: #ff0000;
  color: #fff;
  border: none;
  border-radius: 5px;
  padding: 10px 20px;
  font-size: 18px;
  cursor: pointer;
}

.submit-button:hover {
  background-color: #333;
}
/* Zoom on hover */
.zoom-image {
  transition: transform 0.9s ease-in-out;
}
.zoom-image:hover {
  transform: scale(1.05);
}
.product__thumb__pic img,
.product__details__pic__item img {
    max-width: 100%;
    height: auto;
    object-fit: contain; /* or object-fit: cover; depending on your design */
}
.zoom-image {
            transition: transform 0.9s ease-in-out;
        }

        .zoom-image:hover {
            transform: scale(1.05);
        }
    </style>
 
        <main>
          <!-- Hero Area Start -->
          <section class="hero-area">
            <div class="container">
              <div class="hero-content">
                <h2>Product Details</h2>
              </div>
            </div>
          </section>
      
          <!-- Hero Area End -->
      
          <!-- Breadcrumb Area Start -->
          <section class="breadcrumb-area">
            <div class="container">
              <div class="breadcrumb-content">
                <h1>
                  <%=data.name %>
                </h1>
                <nav>
                  <a href="/">Home</a>
                  <span class="lnr lnr-arrow-right"></span>
                  <a href="/shop">Shop</a>
                  <span class="lnr lnr-arrow-right"></span>
                  <a href="#">Product Details</a>
                </nav>
              </div>
            </div>
          </section>
          <!-- Breadcrumb Area End -->
      
          <!-- Single Product Area Start -->
          <section class="single-product-area">
            <div class="container">
              <div class="row">
               <!-- Main Product Image -->
<div class="col-lg-6 col-md-9">
  <div class="tab-content" style="margin-top: 50px;">
    <% for (let i = 0; i < data.Image.length; i++) { %>
      <div class="tab-pane <% if (i === 0) { %>active<% } %>" id="tabs-<%= i + 1 %>" role="tabpanel">
        <div class="product__details__pic__item">
          <img src="/public/proImage/<%= data.Image[i] %>" alt="<%= data.name %>" class="main-product-image zoom-image">
        </div>
      </div>
    <% } %>
  </div>
</div>

<!-- Thumbnail Images -->
<div class="col-lg-3 col-md-3">
  <ul class="nav nav-tabs" role="tablist">
    <% for (let i = 0; i < data.Image.length; i++) { %>
      <li class="nav-item">
        <a class="nav-link <% if (i === 0) { %>active<% } %>" data-toggle="tab" href="#tabs-<%= i + 1 %>" role="tab">
          <div class="product__thumb__pic">
            <!-- Use a different image source for thumbnails, if available -->
            <img src="/public/proImage/<%= data.Thumbnail && data.Thumbnail[i] ? data.Thumbnail[i] : data.Image[i] %>" alt="<%= data.name %>" class="thumbnail-image zoom-image">
          </div>
        </a>
      </li>
    <% } %>
  </ul>
</div>

        
                <!-- Product Information -->
                <div class="col-lg-3">
                  <div class="product-info">
                    <h3><%= data.name %></h3>
                    <h2>₹ <%= data.price %></h2>
                    <ul class="list">
                      <li><span>Category</span>: <%= data.category.name %></li>
                      <li><span>Availability</span>: <% if (data.quantity > 0) { %> Stock is available <% } else { %> Out of stock <% } %></li>
                    </ul>
                    <p><%= data.description %></p>
        
                    <div class="quantity">
                      <div class="quantity-controls">
                        <button class="quantity-decrease">-</button>
                        <input class="input-number" type="number" value="1" min="1" max="10" />
                        <button class="quantity-increase">+</button>
                      </div>
                    </div>
        
                    <i class="far fa-heart" onclick="addToWishlist('<%= data._id %>')"></i>
        
                    <div class="product-actions">
                      <div class="add_to_cart">
                        <button type="submit" onclick="addToCart('<%= data._id %>')" class="btn btn-danger mx-3">
                          Add To Cart
                        </button>
                      </div>
                    </div>
                  </div>
                </div>
        
              </div>
            </div>
          </section>
        </main>
        <!-- Add this to your HTML head section -->
          <script src="https://cdnjs.cloudflare.com/ajax/libs/jquery/3.6.0/jquery.min.js"></script>
<script src="https://cdnjs.cloudflare.com/ajax/libs/elevatezoom/3.0.8/jquery.elevatezoom.min.js"></script>

        <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11/dist/sweetalert2.all.min.js"></script>
      <script>
  document.addEventListener("DOMContentLoaded", function () {
        // Preload images
        const images = document.querySelectorAll(".zoom-image");
        images.forEach((img) => {
            const src = img.getAttribute("src");
            const image = new Image();
            image.src = src;
        });

        // Initialize Elevate Zoom on the main product image
        $(".zoom-image").elevateZoom({
            zoomType: "inner",
            cursor: "crosshair",
            zoomWindowFadeIn: 1000,
            zoomWindowFadeOut: 1000,
            responsive: true,
            containLensZoom: true,
            gallery: 'thumbnail-gallery',
            galleryActiveClass: 'active',
            imageCrossfade: true,
            loadingIcon: 'https://www.elevateweb.co.uk/spinner.gif',
            onZoomedImageLoaded: function () {
                // Adjust the zoom window position based on the mouse position
                $(".zoomContainer").mousemove(function (e) {
                    var mouseX = e.pageX - $(this).offset().left;
                    var zoomPosition = mouseX / $(this).width();
                    $(".zoomWindow").css("left", zoomPosition * 100 + "%");
                });
            }
        });
    });

</script>

        
        <script>
       function addToCart(id) {
  console.log(id);
  $.ajax({
    url: "/cart",
    method: "post",
    dataType: "json", // Specify that you expect JSON in the response
    data: {
      productId: id, // Use "productId" as the key to match the server-side code
    },
  })
  .done((data) => {
    if (data.success) {
      Swal.fire({
        position: "center",
        icon: "success",
        title: "Product added to cart",
        showConfirmButton: false,
        timer: 1500,
      });
    } else {
      if (data.redirectToLogin) {
        window.location.href = '/login';
      } else {
        // Handle other failure scenarios
        console.error('Failed to add the product to the cart:', data.error);
      }
    }
  })
  .fail((xhr, status, error) => {
    console.error('An error occurred during the add to cart process:', error);
  });
}

      
          // function addToWishlist(id) {
          //   console.log(id);
          //   $.ajax({
          //     url: "/addToWishlist",
          //     method: "post",
          //     encoded: true,
          //     data: {
          //       Id: id,
          //     },
          //   }).success((response) => {
          //     if (response.success) {
          //       Swal.fire({
          //         position: "center",
          //         icon: "success",
          //         title: "Added To Wishlist",
          //         showConfirmButton: false,
          //         timer: 1500,
          //       });
          //     } else if (response.check) {
          //       Swal.fire({
          //         position: "center",
          //         icon: "warning",
          //         title: "Oops...",
          //         text: "Already added to Wishlist!!",
          //       });
          //     } else {
          //       window.location.href = '/login';
          //       }
          //   });
          // } 
          document.addEventListener("DOMContentLoaded", function() {
    // Preload images
    const images = document.querySelectorAll(".zoom-image");
    images.forEach(img => {
        const src = img.getAttribute("src");
        const image = new Image();
        image.src = src;
    });
});

        </script>

<%- include("../layouts/userpartials/footer.ejs") %>
